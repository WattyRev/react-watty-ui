import styled from "styled-components";

/**
 * A styled `<table>` element. Passes all props directly to the `<table>` element.
 */
const Table = styled.table`
  border-collapse: collapse;
`;

/** @component */
export default Table;
